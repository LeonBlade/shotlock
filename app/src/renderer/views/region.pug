extends _extend

block head
    title Shot Lock
    +stylesheet('region')
    +script('region')

block body
    svg.window-dragger
        rect.stroke-black(x='0', y='0', width='100%', height='100%')
        rect.stroke-white(x='0', y='0', width='100%', height='100%')
